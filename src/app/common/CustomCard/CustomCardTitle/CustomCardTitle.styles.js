import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  container: { maxHeight: 60 },
  title: {
    padding: 15,
    paddingLeft: 20,
  },
  rightContainer: { padding: 10, textAlign: 'right' },
}));
