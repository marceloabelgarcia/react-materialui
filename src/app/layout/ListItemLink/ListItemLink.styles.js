import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  item: {
    overflow: 'hidden',
    textOverflow: 'ellipsis',
  },
  icon: {
    maxWidth: 35,
    minWidth: 35,
  },
}));
