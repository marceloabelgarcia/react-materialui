import { connect } from 'react-redux';
import CertifiersDocument from './Certifiers';

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(CertifiersDocument);
