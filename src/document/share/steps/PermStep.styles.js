import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  sectionDividerOptions: { marginBottom: 10 },
  permissionContainer: {
    marginBottom: 10,
    paddingRight: 30,
  },
  permissionTitle: {
    marginTop: 'auto',
    marginBottom: 'auto',
    maxWidth: 150,
  },
  formControl: { maxHeight: 40 },
  formInput: {
    maxHeight: 40,
    fontSize: 14,
  },
  datePicker: {},
  totalItemContainer: { marginTop: 20 },
}));
