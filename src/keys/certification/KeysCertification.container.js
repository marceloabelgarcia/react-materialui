import { connect } from 'react-redux';
import KeysCertification from './KeysCertification';

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(KeysCertification);
