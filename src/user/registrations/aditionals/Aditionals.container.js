import { connect } from "react-redux";
import Aditionals from "./Aditionals";

const mapStateToProps = (/* state */) => ({});

const mapDispatchToProps = (/* dispatch */) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(Aditionals);
